# Реализация Crisha.Scope: HTML/JS/CSS + Django (Python)

**Ответ: ДА, и это ИДЕАЛЬНЫЙ вариант.**
Для задач, связанных с AI и анализом документов (PDF), Django (Python) на голову выше WordPress (PHP).

## Почему Django лучше для этого проекта?

1.  **Экосистема Python:** Самые мощные библиотеки для AI (LangChain, OpenAI, PyTorch) и работы с PDF (`PyMuPDF`, `pdfplumber`) написаны именно на Python.
2.  **Качество обработки документов:** В отличие от PHP, библиотеки Python позволяют вытаскивать из PDF не только текст, но и структуру (таблицы, заголовки), что критически важно для юридического анализа.
3.  **Масштабируемость:** Django отлично подходит для создания API, которое потом можно использовать и для мобильного приложения, и для Telegram-бота.

## 🏗 Архитектура решения

Мы сохраняем идею "чистого фронтенда", но меняем бэкенд на Python.

### 1. Frontend (Клиентская часть)
Остается таким же легким и независимым:
*   **HTML:** Лендинг с формой загрузки.
*   **CSS:** Tailwind CSS или ваш кастомный CSS.
*   **JS:** Отправляет файл на API бэкенда (`POST /api/analyze/`) и ждет ответ.

### 2. Backend (Django / Django REST Framework)
*   **Прием файла:** Django View принимает PDF.
*   **Обработка:**
    *   `PyMuPDF` (fitz) извлекает текст.
    *   Скрипт проверяет длину текста, наличие OCR слоя.
*   **AI Анализ:**
    *   Отправляем текст в OpenAI API (GPT-4o) с системным промптом юриста.
    *   Получаем структурированный ответ (JSON: оценка риска, список проблем).
*   **Ответ:** Возвращаем JSON фронтенду.

## ⚠️ Что нужно учесть (Сложности)

### 1. Хостинг (Деплоймент)
Django нельзя просто закинуть на дешёвый Shared-хостинг за 100 рублей (как WordPress). Вам понадобится **VPS** (Виртуальный сервер).
*   **Решение:** Купить VPS (например, Timeweb, Beget, Reg.ru) за ~300-500 руб/мес.
*   **Настройка:** Потребуется настроить Linux (Ubuntu), Nginx, Gunicorn. Это чуть сложнее, чем "нажать кнопку Install WP", но дает полный контроль.

### 2. Асинхронность (Celery)
Если документ большой, анализ может занять 30-50 секунд.
*   **Решение:** Чтобы пользователь не ждал "зависшего" запроса, нужно использовать очередь задач (Celery + Redis).
    1.  Юзер грузит файл -> Сервер отвечает "Принял, ID=555".
    2.  На фоне (Celery) идет анализ.
    3.  JS каждые 2 секунды спрашивает: "Задача 555 готова?".
    4.  Когда готова — показываем результат.

## 🚀 План разработки на Django

1.  **Backend (База):**
    *   Установка Django + DRF.
    *   Настройка модели `Contract` (хранение файла и статуса).
    *   API Endpoint `/upload/`.

2.  **AI модуль (Core):**
    *   Скрипт `analyzer_service.py`: функция `analyze_pdf(file_path)`.
    *   Интеграция OpenAI SDK.

3.  **Frontend (Интерфейс):**
    *   Верстка формы.
    *   JS-логика polling-а (опроса статсуса).

## Резюме
Использование **Django** — это **профессиональный выбор** для AI-стартапа. Это позволит вам не упереться в потолок возможностей PHP через месяц работы.

Я готов начать с создания структуры Django-проекта, если у вас есть установленный Python. Или мы можем начать с верстки фронтенда, пока вы решаете вопрос с сервером.
