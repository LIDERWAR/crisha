# Реализация Crisha.Scope: HTML/JS/CSS + WordPress

**Ответ: ДА, это полностью возможно.** 
Такой подход (Headless WP или просто использование WP как бэкенда для кастомного фронтенда) — очень распространенная практика.

## 🏗 Архитектура решения

1.  **Frontend (Клиентская часть):**
    *   **HTML:** Каркас страницы (форма загрузки, результаты).
    *   **CSS:** Стилизация (анимации, адаптивность).
    *   **JS:** Логика отправки файла (AJAX/Fetch), обработка ответа от сервера, отрисовка отчета.
    *   *Это работает как обычная статичная страница, которую WordPress просто "отдает" пользователю.*

2.  **Backend (Серверная часть на WordPress):**
    *   **PHP:** Обработка загруженного файла.
    *   **WordPress REST API (или `admin-ajax.php`):** Прием файла от JS.
    *   **Логика:** Скрипт получает PDF -> Извлекает текст -> Отправляет в OpenAI API -> Возвращает ответ фронтенду.

## ⚠️ Основные сложности и риски

При реализации "тяжелой" логики (работа с PDF и AI) на PHP/WordPress вы столкнетесь со следующими проблемами:

### 1. Парсинг PDF на PHP (Самая большая боль)
*   **Проблема:** PHP не очень хорош для чтения PDF. Библиотеки типа `Smalot/PdfParser` или `TCPDF` могут "давиться" сложными документами (сканами, кривой версткой), выдавая "мусор" вместо текста. Python в этом плане гораздо мощнее (`pdfplumber`, `PyMuPDF`).
*   **Решение:**
    *   Использовать **OpenAI Assistants API** (он умеет принимать файлы напрямую, но это может быть дороже и медленнее).
    *   Использовать внешний API для OCR/парсинга (например, Google drive API или специализированные сервисы).
    *   Смириться с тем, что сложные PDF (сканы) читать не получится без OCR.

### 2. Тайм-ауты сервера (Timeout Issues)
*   **Проблема:** Анализ договора через GPT-4 может занимать **30-60 секунд**. Стандартный веб-сервер (Nginx/Apache) и PHP часто имеют ограничение на выполнение скрипта в 30 секунд. Результат: Пользователь увидит ошибку "504 Gateway Time-out".
*   **Решение:**
    *   Настройка сервера (увеличение `max_execution_time`).
    *   Асинхронная архитектура: JS отправляет файл -> Сервер говорит "Принял, ID заявки 123" -> JS каждые 2 секунды спрашивает "Готово?" (Polling).

### 3. Безопасность загрузки файлов
*   **Проблема:** Разрешить пользователям грузить файлы на ваш сервер — всегда риск. Могут загрузить вирус под видом PDF.
*   **Решение:**
    *   Строгая валидация MIME-типов.
    *   Переименование файлов при сохранении.
    *   Удаление файлов сразу после анализа (не хранить их на сервере).

### 4. Конфликты плагинов
*   **Проблема:** Сторонние плагины безопасности или кэширования могут блокировать ваши AJAX-запросы к API.

---

## 🛠 План реализации на WordPress

Если мы идем этим путем, схема будет такой:

1.  **Верстка:** Создаем 3 файла (`index.html`, `style.css`, `script.js`).
2.  **Интеграция в WP:**
    *   Создаем папку в теме (например, `/crisha-scope/`).
    *   Создаем шаблон страницы в WP (Page Template), который просто подключает эти файлы.
3.  **Бэкенд (PHP):**
    *   Пишем функцию в `functions.php` или отдельный мини-плагин.
    *   Хук для AJAX:
        ```php
        add_action('wp_ajax_analyze_contract', 'crisha_analyze_contract_handler');
        add_action('wp_ajax_nopriv_analyze_contract', 'crisha_analyze_contract_handler');
        ```
    *   В функции `crisha_analyze_contract_handler`:
        1.  Проверяем файл `$_FILES`.
        2.  Извлекаем текст (библиотекой `smalot/pdfparser`).
        3.  Отправляем cURL запрос в OpenAI.
        4.  Отдаем JSON.

## Вердикт
**Можно ли так сделать?** Да.
**Сложно ли?** Средне. Основной затык будет в качественном извлечении текста из PDF на чистом PHP.

**Альтернатива (Гибрид):**
Frontend на WP + Backend на Python (FastAPI) на поддомене `api.crisha.ru`.
WP отдает красивую картинку, а JS шлет файл на Python-сервер, который идеально парсит PDF и быстро отвечает. Это **идеальный** вариант архитектуры.
